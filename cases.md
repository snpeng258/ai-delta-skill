# 案例

一行一条：`日期｜标签｜一句话现象｜缺了什么｜ob:<source>/<session_id>`

标签：`(a)` 看起来对但不是我要的 · `(b)` 修到了错的层 · `(c)` 层次混在一起 · `(d)` 量超过我能读的 · `(+)` 明显高于中位

内容留在原 session 里，这里只放**能让我想起来的那句话**。标签和现象是检索入口，`ob:` 用来跳回原文。

Cursor 进库后 `source` 仍是 `claude`，靠标题前缀 `[Cursor]` 区分。`overview().current.session_id` 对 Cursor 经常是 `null`，用本项目最新那条 `[Cursor]` session。不要写「本次对话」。

---

（倒序，最新在上）

- 2026-08-22｜(a)｜让它加载自己的 writing-engine，它检索不到项目就在 skill 仓库里建了一套同名的 AI-Delta 写作目录；我要的是加载已有 skill，不是再造一份｜缺：没找到就该停/问路径，加载 ≠ 按同名新建｜`ob:claude/856d3214-5d6c-4ac4-873b-822f1c964a27`
- 2026-08-21｜(a)(c)｜让它加载 skill，它把 phrasing 全文当约束；我要的全文加载发生在我整理 case→phrasing 时，不是每次对话｜缺：笔记本不该在 skill 文件夹里；「全文加载」指人读 cases，不是 agent 读 phrasing｜`ob:claude/856d3214-5d6c-4ac4-873b-822f1c964a27`
- 2026-08-21｜(a)｜让它给这个库搭骨架，它造了 13 个文件、每份带六字段模板——看起来齐全，但我要的是说一句话就自动长出来的东西｜缺：反目标里没写"宁可漏，也不要让我填表"｜`ob:claude/856d3214-5d6c-4ac4-873b-822f1c964a27`

# 案例

一行一条：`日期｜标签｜一句话现象｜缺了什么｜session`

标签：`(a)` 看起来对但不是我要的 · `(b)` 修到了错的层 · `(c)` 层次混在一起 · `(d)` 量超过我能读的 · `(+)` 明显高于中位

内容留在原 session 里，这里只放**能让我想起来的那句话**。OB 能全文检索，所以 session id 不携带信息，标签和现象才是检索入口。

> OB 目前只索引 codex。Cursor 的对话在接口拓展完之前多写半句现场，否则以后翻不回去。

---

（倒序，最新在上）

- 2026-08-21｜(a)｜让它给这个库搭骨架，它造了 13 个文件、每份带六字段模板——看起来齐全，但我要的是说一句话就自动长出来的东西｜缺：反目标里没写"宁可漏，也不要让我填表"｜cursor·本次对话

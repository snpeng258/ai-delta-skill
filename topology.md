# 拓扑

一类项目一节。**标题必须自带一句话摘要**：`## 项目类型 · 这类项目的核心问题`——所有标题连起来就是索引，用 `rg '^## '` 现场生成。

每层一行：`层名 — 判据：什么样的改动算这一层`

**判据比层名重要。** 层名帮不了定位，判据能——"这个问题在数据形状层，不在渲染层"。

定位错了就在那一节末尾加一行，比记对的更有用。

---

## 带前后端的网站 · 同一份数据在几处各有一个形状

bug 出在接缝上，不在某层内部。

- L1 数据形状 — 判据：会不会让同一个东西在两处的字段不一样
- L2 存取 — 判据：关心数据从哪来往哪去，不关心它长什么样
- L3 业务规则 — 判据：换一套 UI、换一个数据库，还得留着吗
- L4 传输 — 判据：改了之后另一侧必须同步改
- L5 渲染 — 判据：只影响看到什么，不影响是什么
- L6 环境交付 — 判据：在我本机不会有任何表现差异
- 最小结构：L1 L2 L5 L6。L3 L4 多数时候是过劲，它默认会都建起来。
- 常见误放：形状不一致被修在 L5（加个 `?? ''`），错误消失在下游。

---

## 内容型 React 博客 · 同一篇文章要在主页、列表和详情页保持同一个内容契约

- L1 内容形状 — 判据：主页卡片、文章列表和详情页是否都从同一个 `Article` / `ArticleSection` 字段读取
- L2 内容存取 — 判据：文章从本地 TypeScript、Markdown、CMS 还是 API 来，但不关心页面如何显示
- L3 路由映射 — 判据：URL、`slug` 和页面之间的对应是否稳定，刷新 `/articles/:slug` 是否仍能找到正确文章
- L4 页面编排 — 判据：决定 Home、Articles、ArticleDetail、About、404 等页面如何组合，不承载文章正文
- L5 内容渲染 — 判据：把标题、摘要、段落、代码、重点句和目录转换成可阅读的页面结构
- L6 视觉系统 — 判据：只影响排版、色彩、响应式和 React Bits 动效，不改变文章字段和 URL 语义
- L7 环境交付 — 判据：构建产物、图片路径和静态托管的路由回退是否让深层文章地址可以直接打开
- 最小结构：L1 L2 L3 L4 L5 L6 L7 都有清楚边界；当前先用本地内容完成 L1-L6，L2 的 API 版本可以后置。
- 当前误放：`article.tsx` 里的段落含有 JSX，暂时同时承担 L1 和 L5；新增文章前应先把内容格式与渲染组件拆开。

# 词汇

一个词一节。**标题必须自带一句话摘要**：`## 词 · 一句话摘要`——所有标题连起来就是索引，用 `rg '^## '` 现场生成。

## 每节固定四行，再叠加

首次见到时写四行，以后只往「叠加」里加，不改这四行（除非当时记错了）：

- **外行说法：** 我一开始接触它时，用自己的话怎么理解。允许笨。
- **首次：** `ob:<source>/<session_id>` ，有具体一句再加 `#<uuid>`。用来回到原文，不要把原文抄进来。
- **搞混过：** 没有就写「无」。有就写和哪个概念搞混、后来怎么分开。
- **拓扑：** 没有就写「无」。有就写 `《topology.md 的节标题》 · L<n> <层名>`，并留下检索命令。

叠加一行：`日期｜这次它指什么，不指什么｜ob:...`

## Agent 怎么找回拓扑

词条里的「拓扑」字段已经写了节标题和层号。分析时：

1. `rg '^## <节标题>' topology.md` 定位那一节
2. 读该节里 `L<n>` 那一行的判据
3. 不要靠记忆猜层名，标题变了以 `topology.md` 为准

## Obelisk 标记

格式 `ob:<source>/<session_id>`，有具体那一句再加 `#<uuid>`。回原文用 query 里的 `thread(sessionId)` 或 `context(uuid)`，**不要把原文抄进词条**。

三条实测出来的坑：

- **Cursor 的对话 `source` 记成 `claude`**，不是 `cursor`。靠标题前缀 `[Cursor]` 区分。
- **一次对话可能被拆成多个 session。** 换工作区根目录就会新开一条，`project` 不同、`id` 不同。写标记时用当时所在项目的那一条。
- **中文检索要用短词。** `验证` 能命中，`验证带宽` 命中不到——CJK 连写会被当成一个 token。词条标题里的词请尽量短。

```bash
obelisk --search "<短词>" --nonce "$(uuidgen)"
```

---

## 局部搜索算子 · 为什么它总爱打补丁

- 外行说法：AI 比较笨，所以只会在现有代码上打补丁，看不到大图。
- 首次：`ob:claude/856d3214-5d6c-4ac4-873b-822f1c964a27`（Cursor 对话，标题带 `[Cursor]`）
- 搞混过：和「视野太小」「温度导致随机」搞混。它能看到我给它的全部，问题在优化目标，不在视野和采样。
- 拓扑：无。这是跨项目的 AI 行为词，不落在某个网站的某一层。

### 叠加

- 2026-08-21｜指它沿"让这个 diff 通过"下降，上下文窗口就是它的全世界，所以初值决定收敛；不指它视野小，也不是随机性问题｜`ob:claude/856d3214-5d6c-4ac4-873b-822f1c964a27`
